export interface IPersonDTO {
    indexId?: number,
    address : string,
    creditLimit: number,
    registerDate: Date,
    updateDate: Date,
    comments: string
}

// regras de negócio da entidade